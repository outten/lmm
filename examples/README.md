# Examples

Sample questions and the code-first responses the model is trained to
produce. Each example demonstrates a different tier of capability.

- `tier1_arithmetic.md`  — basic integer arithmetic
- `tier5_calculus.md`    — symbolic + numerical integration
- `tier8_correlation.md` — data analysis with pandas + matplotlib

These are also valid input formats for the eval harness.